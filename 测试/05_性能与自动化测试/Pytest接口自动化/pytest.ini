[pytest]
testpaths = testcases
addopts = -ra -q --tb=short
# 并行执行示例（需 pytest-xdist）：addopts 追加 -n auto
# HTML 报告示例（需 pytest-html）：--html=reports/report.html --self-contained-html

[markers]
smoke: 冒烟用例，核心主流程
p0: 高优先级用例，回归必测
regression: 回归套件用例
